meta {
  name: revoke
  type: http
  seq: 3
}

delete {
  url: {{apiUrl}}/api-keys/{{apiKeyId}}
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("responds 204", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
